# # f'{value:{width}.{precision}}

# candidate_votes = int(input("How many votes did the candidate get in the election? "))
# total_votes = int(input("What is the total number of votes in the election? "))
# message_to_candidate = (
#     f"You recieved {candidate_votes} votes in the election."
#     f"You recieved {candidate_votes/total_votes*100:.1f}% of the votes in the election.")

# print(message_to_candidate)

## includes comma separator for 2nd row
# message_to_candidate = (
#     f"You received {candidate_votes:,} number of votes. "
#     f"The total number of votes in the election was {total_votes:,}. "
#     f"You received {candidate_votes / total_votes * 100:.2f}% of the total votes.")

# print(message_to_candidate)


# prints 'message' for all 3 counties
counties_dict = {"Arapahoe": 422829, "Denver": 463353, "Jefferson": 432438}

for county, voters in counties_dict.items():
    message=(f"{county} county has {voters:,} registered voters")
# print command needs to be SAME INDENT as for loop
    print(message)



## prints 'message' for all 3 counties with layered dictionary
# voting_data = [{"county":"Arapahoe", "registered_voters": 422829}, 
# {"county":"Denver", "registered_voters": 463353}, 
# {"county":"Jefferson", "registered_voters": 432438}]

# for i in range(len(voting_data)):
#     message = (f"{voting_data[i]['county']} county has {voting_data[i]['registered_voters']:,} registered voters.")
#     print(message)